name: Release

run-name: Release (${{ inputs.bump }}) from ${{ github.ref_name }}

on:
  workflow_dispatch:
    inputs:
      bump:
        description: Version bump type
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
        default: patch

permissions:
  contents: write

jobs:
  release:
    uses: otfabric/.github/.github/workflows/go-release.yml@v1
    with:
      bump: ${{ inputs.bump }}
      release-name-prefix: go-tpkt
      update-major-tag: true
    secrets: inherit
